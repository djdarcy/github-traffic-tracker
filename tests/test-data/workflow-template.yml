name: Track Downloads & Clones
on:
  schedule:
    - cron: '0 3 * * *'
  workflow_run:
    workflows: ["CI"]
    types: [completed]
  workflow_dispatch:
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Collect traffic
        run: |
          cat <<'JS'
          version: "0.0.1",
          JS
